const crearMetodo = require('./crearMetodoP');
const listarMetodos = require('./listarMetodosP');
const updateMetodo = require('./actualizarMetodoP');
const deleteMetodo = require('./eliminarMetodoP');
const obtenerMetodo = require('./obtenerMetodoP');

module.exports = {
    crearMetodo,
    listarMetodos,
    updateMetodo,
    deleteMetodo,
    obtenerMetodo,
}