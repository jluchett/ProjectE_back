const crearMetodo = require('./crearMetodoP');
const listarMetodos = require('./listarMetodosP');
const updateMetodo = require('./actualizarMetodoP');

module.exports = {
    crearMetodo,
    listarMetodos,
    updateMetodo,
}